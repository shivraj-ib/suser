{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, { 'action': path('edit_user',{'id':id}), 'attr': { 'class': 'fos_user_profile_edit','novalidate': 'novalidate' } }) }}
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'profile.edit.submit'|trans }}" />
    </div>
{{ form_end(form) }}
